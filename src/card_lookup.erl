-module(card_lookup).
-export([get/1]).

% this module works in conjunction with the observer module;  when a player inputs a card id, the module lookups the implementation definition and 
% returns it

% implements the card lookup modules
get(_) ->
    {}.
